@model IEnumerable<BugBusinessLayer.ProgramModel>

@{
    ViewBag.Title = "Index";
    //WebGrid grid = new WebGrid(Model);
}

<h3>Programs</h3>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
@*@grid.GetHtml(columns: new[]
{
    grid.Column("Id"),
    grid.Column("ProgramName"),
    grid.Column("Version"),
    grid.Column("Release"),
    grid.Column("",header:"Actions",
                    format:@<text> 
@Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
@Html.ActionLink("Details", "Details", new { id=item.Id }) |
@Html.ActionLink("Delete", "Delete", new { id=item.Id })
                            </text>)
})*@
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Program)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Version)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Release)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Program)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Version)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Release)
        </td>
        <td style="white-space:nowrap">            
                @*@Html.ActionLink("Edit", "Edit", new { id = item.Id }) |*@ 
                @Html.ActionLink("Areas", "Areas", new { id = item.Id })
                @using (Html.BeginForm("Delete", "Program", new { id = item.Id }, FormMethod.Post))
                {
                    <input type="image" style="width:18px;height:18px;display:inline-block" src="~/Images/delete.png" 
                           onclick="return confirm('Are you sure?')" />
                }
                @*@Ajax.ActionLink("Delete", "Delete", "Program", new { id = item.Id }, new AjaxOptions { HttpMethod = "Post" })*@        
        </td>
    </tr>
}
</table>

@*<script type="text/javascript">
    $("deletebut").click(function () {
        var form = $(this).parents('form:first');
        form.submit();
    });
</script>*@
